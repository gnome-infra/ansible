{% if dovecot_ssl | default(false) %}
ssl = required
ssl_cert = <{{ dovecot_ssl_cert_path }}
ssl_key = <{{ dovecot_ssl_key_path }}
ssl_options = no_compression
ssl_prefer_server_ciphers = yes
ssl_min_protocol = TLSv1.2
ssl_dh = </etc/dovecot/dh2048.pem
{% endif %}
