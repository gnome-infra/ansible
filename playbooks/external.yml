---
- hosts: gitlab-runners
  gather_facts: true
  remote_user: root
  roles:
   - external_base
   - rsyslog
   - sshguard
   - gitlab-runner

- name: gather facts
  hosts: gitlab-runners,gitlab-runners-freebsd,external
  remote_user: root
  gather_facts: true
  tasks: []

- hosts: gitlab-runners-freebsd
  gather_facts: true
  remote_user: root
  roles:
   - freebsd_base
   - freebsd_pot
   - gitlab_runner_freebsd

- hosts: hetzner-3
  gather_facts: true
  remote_user: root
  roles:
   - external_prometheus
